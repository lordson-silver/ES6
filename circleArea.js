var r = prompt("Input a radius");
function circleArea(r) {
  return Math.floor(Math.PI * Math.pow(r, 2));
}

alert(circleArea(r));
console.log(circleArea(r));

// GABBIE
const cirArea = (r) => Math.floor(Math.PI * Math.pow(r, 2));
alert(cirArea(r));
